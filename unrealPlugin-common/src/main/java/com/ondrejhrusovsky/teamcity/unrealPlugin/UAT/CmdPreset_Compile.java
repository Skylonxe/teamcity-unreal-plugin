package com.ondrejhrusovsky.teamcity.unrealPlugin.UAT;

public class CmdPreset_Compile extends CmdPreset_BuildCookRun {
    CmdPreset_Compile()
    {
        friendlyName = "Compile (C# tools)";
        /*arguments = new ArrayList<UATArgument>(Arrays.asList(
                new Arg_Clean()
                (NO)COMPILE EDITOR

        ));*/
    }
}
